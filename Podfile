# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

project 'Example'
workspace 'Appear'

target 'Example' do
  use_frameworks!

  pod 'Appear', path: './'
  pod 'SwiftLint'

end
